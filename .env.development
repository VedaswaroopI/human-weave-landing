# Development Environment Variables
# These are only used in development mode (npm run dev)

# Disable component tagger if it causes preview issues
# Set to false to enable the tagger
VITE_DISABLE_TAGGER=false
